#!/bin/bash

COMMAND=$1

if [ -z "$COMMAND" ]; then
    echo
    echo "GRC: No command passed."
    echo
    exit 1
fi

OLD_USER_DIR=$(pwd)

cd /opt/grc/GitHub-Repo-Creator

source venv/bin/activate
python ./.program-files/main.py $@
deactivate

cd $OLD_USER_DIR
