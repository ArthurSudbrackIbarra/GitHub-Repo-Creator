name: "Cookies"
description: "I really like cookies!"
private: true
autoPush: true

# Collaborators list (Optional).
collaborators:
  - collaborator:
      name: "brun0-znx" # Collaborator name. (Required).
      permission: "pull" # Collaborator permission -> push/pull/admin. (Optional, default is admin).
